{
  "name": "backend",
  "version": "1.0.0",
  "description": "CampusPay backend API (Algorand Testnet)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec \"ts-node src/index.ts\"",
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/cors": "^2.8.19"
  }
}
